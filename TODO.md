1) add functionality to the `Save changes` button
2) add support thread/messages communication
3) add the manager home panel
4) fix dangling status indicators in login page upon startup
5) redirect back to login when token expires
6) update `requirements.txt` to cite exact versions