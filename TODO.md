1) add support thread/messages communication
2) add `title` column to the thread table
3) make the messages style on booker's thread match the one in manager-home
4) make it so that when the thread is closed the `Reply` button is disabled
5) make it so that the open threads are displayed first
6) the capacity of a tour should not be lessened (or only lessened if there is still room)
7) same goes for the start time as in it should not be preponed. (or only if the date is after today)
8) add image selection option
9) add a reset password option in the `booker-home` page
10) update `requirements.txt` to cite exact versions