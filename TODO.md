1) add support thread/messages communication
2) the capacity of a tour should not be lessened (or only lessened if there is still room)
3) same goes for the start time as in it should not be preponed. (or only if the date is after today)
4) add a reset password option in the `booker-home` page
5) update `requirements.txt` to cite exact versions