1) add support thread/messages communication
2) add `title` column to the thread table
3) the capacity of a tour should not be lessened (or only lessened if there is still room)
4) same goes for the start time as in it should not be preponed. (or only if the date is after today)
5) add image selection option
6) add a reset password option in the `booker-home` page
7) update `requirements.txt` to cite exact versions